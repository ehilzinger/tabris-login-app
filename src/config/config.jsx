import { Font, Color } from 'tabris';

// Fonts
const font_large = Font.from("normal thin 32px sans-serif");
const font_info = Font.from("normal thin 20px sans-serif");

// Colors
const heading_color = new Color(235, 163, 56);
const button_color_action = new Color(235, 143, 52);
const info_color = new Color(235, 98, 52);

export { font_large, font_info, heading_color, button_color_action, info_color };
